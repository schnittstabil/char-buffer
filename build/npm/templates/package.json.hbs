{
  "name": "{{{name}}}",
  "version": "{{{version}}}",
  "description": "{{{description}}}",
  "homepage": "{{{homepage}}}",
  "keywords": [
    {{#each keywords}}{{#if @index}},
    {{/if}}"{{{this}}}"{{/each}}
  ],
  "author": "{{{author}}}",
  "license": "{{{license}}}",
  "repository": {
    "type": "{{{repository.type}}}",
    "url": "{{{repository.url}}}"
  },
  "bugs": {
    "url": "{{{bugs.url}}}"
  },
  "preferGlobal": false,
  "main": "char-buffer.js",
  "scripts": {
    "test": "node_modules/.bin/mocha --recursive"
  },
  "engines": {
    "node": "{{{engines.node}}}"
  },
  "dependencies": {
    {{#each dependencies}}{{#if @index}},
    {{/if}}"{{@key}}": "{{{this}}}"{{/each}}
  },
  "devDependencies": {
    "expect": "{{{devDependencies.expect}}}",
    "mocha": "{{{devDependencies.mocha}}}"
  }
}
