{
  "name": "{{{name}}}",
  "version": "{{{version}}}",
  "description": "{{{description}}}",
  "homepage": "{{{homepage}}}",
  "keywords": [
    {{#each keywords}}{{#if @index}},
    {{/if}}"{{{this}}}"{{/each}}
  ],
  "author": "{{{author}}}",
  "license": "{{{license}}}",
  "repository": {
    "type": "{{{repository.type}}}",
    "url": "{{{repository.url}}}"
  },
  "bugs": {
    "url": "{{{bugs.url}}}"
  },
  "preferGlobal": false,
  "main": "./{{{name}}}.js",
  "scripts": {
    "test": "grunt test",
    "postinstall" : "cd test && ../node_modules/.bin/bower install"
  },
  "engines": {
    "node": "{{{engines.node}}}"
  },
  "dependencies": {
  },
  "devDependencies": {
    "bower": "{{{devDependencies.bower}}}",
    "grunt": "{{{devDependencies.grunt}}}",
    "grunt-saucelabs": "{{{devDependencies.[grunt-saucelabs]}}}",
    "grunt-contrib-connect": "{{{devDependencies.[grunt-contrib-connect]}}}",
    "grunt-contrib-watch": "{{{devDependencies.[grunt-contrib-watch]}}}"
  }
}
