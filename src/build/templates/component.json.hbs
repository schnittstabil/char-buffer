{
  "name": "{{{name}}}",
  "repo": "schnittstabil/{{{name}}}",
  "description": "{{{description}}}",
  "version": "{{{version}}}",
  "keywords": [
    {{#each keywords}}{{#if @index}},
    {{/if}}"{{{this}}}"{{/each}}
  ],
  "dependencies": {
    {{#each dependencies}}{{#if @index}},
    {{/if}}"{{@key}}": "{{{this}}}"{{/each}}
  },
  "development": {},
  "license": "MIT",
  "main": "index.js",
  "scripts": [
    {{#each scripts}}{{#if @index}},
    {{/if}}"{{{this}}}"{{/each}}
  ]
}